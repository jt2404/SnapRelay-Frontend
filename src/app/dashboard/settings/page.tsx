import { Settings } from "lucide-react";
import { ComingSoonPage } from "@/components/shared/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoonPage
      title="Settings"
      icon={Settings}
      description="Studio and account settings are coming in the next build phase."
    />
  );
}
