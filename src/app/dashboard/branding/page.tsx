import { Palette } from "lucide-react";
import { ComingSoonPage } from "@/components/shared/coming-soon";

export default function BrandingPage() {
  return (
    <ComingSoonPage
      title="Branding"
      icon={Palette}
      description="Logo upload, brand colors, fonts, and gallery templates are coming in the next build phase."
    />
  );
}
